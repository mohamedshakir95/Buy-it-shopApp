class Product {
  String pName, pPrice, pDescription, pCategory, pLocation,pId;
  Product({
    this.pId,
    this.pLocation,
    this.pCategory,
    this.pDescription,
    this.pName,
    this.pPrice,
  });
}
